# Lo que DanPlay necesita para funcionar. Solo dependencias directas: las que
# arrastran (starlette, pydantic, httpx2, certifi...) las resuelve pip.
#
# Antes esto era la salida de `pip freeze`, con las herramientas de
# construccion dentro: `pip install -r requirements.txt` exigia tener el
# compilador de Rust para nada. Eso vive ahora en requirements-dev.txt.
fastapi==0.141.1
uvicorn==0.52.4
openai==3.8.0
mutagen==1.48.1
RapidFuzz==3.14.6
yt-dlp==2026.8.19
pyacoustid==1.3.1
python-dotenv==1.2.3
watchdog==6.0.0
# rutas de datos y papelera del sistema en Linux, Windows y macOS
platformdirs==4.11.7
Send2Trash==2.1.0
