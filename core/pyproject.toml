[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "danplay_core"
version = "1.0.0"
requires-python = ">=3.13"

[tool.maturin]
features = ["pyo3/extension-module"]
