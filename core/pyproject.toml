[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "danplay_core"
# la del workspace de Cargo (Cargo.toml de la raiz), que maturin lee
dynamic = ["version"]
requires-python = ">=3.13"

[tool.maturin]
features = ["pyo3/extension-module"]
