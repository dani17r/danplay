{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "projection",
  "description": "Permisos de la ventana de proyeccion: la letra en grande para el proyector. Necesita los eventos (sigue lo que suena), poner y quitar la pantalla completa y leer la letra del nucleo.",
  "windows": ["projection"],
  "permissions": [
    "core:event:default",
    "core:window:allow-set-fullscreen",
    "core:window:allow-is-fullscreen",
    "proyeccion"
  ]
}
